defaults:
    recalibrate: true
    pad: true
    clipy: false
    mean_match: true
    n_jobs: -1
    normalize: true     # sweep figures as fractions of the baseline (PI)

simulation:
    seed: 42
    n_samples: 2_048
    kernel_dim: 0
    treatment_dim: 32
    # a 4-dim standard-Gaussian instrument reaching X through a rank-4 map: an
    # INCOMPLETE IV that identifies 4 of the 32 directions and nothing else
    iv: 4
    n_experiments: 8
    sweep_samples: 128
    methods: [PI+INV, PI, PI+IV, PI+INV+IV, DA+PI, DA+PI+IV, PI&DA+PI, PI&DA+PI+IV]
    augmentation: translate
    experiment:
        query: true
        sweep:
            param:  [epsilon, gamma, trS, n, m]
            metric: [coverage, approx_error, worst_error, width]
